# Supabase Configuration
# Get these values from your Supabase project dashboard > Settings > API
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase Auth Configuration
# These are the same as above, but make sure they're properly set for authentication
# The middleware and auth components will use these environment variables

# Google Cloud Configuration (existing)
# These should already be configured for your TTS/STT functionality
GOOGLE_CLOUD_CLIENT_EMAIL=your_service_account_email
GOOGLE_CLOUD_PRIVATE_KEY=your_private_key
GOOGLE_CLOUD_PROJECT_ID=your_project_id
